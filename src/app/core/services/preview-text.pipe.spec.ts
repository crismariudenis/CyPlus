import { PreviewTextPipe } from './preview-text.pipe';

describe('PreviewTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewTextPipe();
    expect(pipe).toBeTruthy();
  });
});
